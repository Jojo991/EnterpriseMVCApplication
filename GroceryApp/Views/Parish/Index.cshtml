@model IEnumerable<GroceryApp.Models.Domain.Parish>

@{
    ViewBag.Title = "Index";
    Layout = "~/views/shared/_layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ParishID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ParishID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ParishID })
        </td>
    </tr>
}

</table>
