@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    [ @Html.ActionLink("Log Off", "LogOff", "Account") ]</text>
}
else {
   <text>Hi Guest
    [ @Html.ActionLink("Log On", "LogOn", "Account") ] to continue</text>
}
